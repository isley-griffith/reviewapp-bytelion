// firebase uid
// check if post has property that corresponds to currentUser uid
// if doesn't, update upvote count
// if does, don't do anything
// need separate function for reducing count if it does

// json object is the same as hashmap, so it's much faster than the array

